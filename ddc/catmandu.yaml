---
importer:
  marcxml:
    package: MARC
    options:
      type: XML
  ndjson:
    package: JSON
    options:
      line_delimited: 1

exporter:
  marcxml:
    package: MARC
    options:
      type: XML
  ndjson:
    package: JSON
    options:
      line_delimited: 1

